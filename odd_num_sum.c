#include <stdio.h>
#include <conio.h>

int main() {
    int n, sum;

    printf("Enter the value of n.(n is an odd number) :");
    scanf("%d",&n);

    sum=n*n;

    printf("sum =%d",sum);

    getch();
    return 0;
}